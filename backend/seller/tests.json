{
  "tests": [
    {
      "name": "Get seller stats without auth",
      "method": "GET",
      "path": "/?endpoint=stats",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "string"
      },
      "bodyMatcher": "partial"
    },
    {
      "name": "Login with invalid credentials",
      "method": "POST",
      "path": "/?endpoint=login",
      "body": {
        "email": "wrong@seller.com",
        "password": "wrongpass"
      },
      "expectedStatus": 401,
      "expectedBody": {
        "error": "string"
      },
      "bodyMatcher": "partial"
    }
  ]
}